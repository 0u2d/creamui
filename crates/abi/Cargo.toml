[package]
name = "creamui-abi"
description = "Plain #[repr(C)] ABI types shared by creamui-ffi (producer) and creamui-dynamic (consumer), with no engine dependency"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
